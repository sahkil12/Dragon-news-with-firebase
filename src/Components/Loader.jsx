import { BeatLoader } from "react-spinners";

const Loader = () => {
  return (
    <div className="flex items-center my-30 justify-center">
     <BeatLoader size={18} ></BeatLoader>
    </div>
  );
};

export default Loader;
